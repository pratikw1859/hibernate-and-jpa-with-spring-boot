server:
  port: 2019
spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    properties:
      hibernate:
        generate_statistics: true
        format_sql: true
    show-sql: true
logging:
  level:
    org.hibernate.stat: debug
    org.hibernate.type: debug    
